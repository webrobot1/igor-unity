# Ведомая копия. Источник — /var/www/html/game/.claude/hooks/lib/contours.py в серверном
# репозитории; копию перезаписывает его хук при каждой правке источника. Правка копии теряется
# молча — вносить её в источник. Серверного репозитория нет под рукой → назвать нужную правку
# пользователю.

"""Корни ВНЕШНИХ контуров проекта — репозиторий клиента Unity и репозиторий игрового узла — одним
носителем на все хуки серверной стороны.

Читателей трое, и вывод из корня они делают разный: гейт владения путями (`skill-guard.sh`)
опознаёт по нему ведомую копию, зеркало сводов (`client-skills-mirror.sh`) кладёт по нему копии,
сводка накопления (`audit-backlog.sh`) меряет по нему дельту клиента. Литерал, стоявший в каждом
из них порознь, разошёлся бы молча: переезд контура правил бы один хук, а остальные два продолжали
бы смотреть в пустой путь. Оттого корень живёт здесь одним экземпляром; хуку, читающему его из
bash, модуль печатает значение вызовом скриптом: `python3 contours.py client|node`.

Пути — настройка среды разработки (`.claude/settings.json` объявляет те же контуры дополнительными
каталогами), из неё не читаются: там стоят подкаталоги зоны записи, не корни репозиториев.

Своего набора самопроверки у модуля нет: значения — два литерала, читают их наборы вызывающих,
подменяя в песочнице копию модуля.
"""
import sys

CLIENT_ROOT = "/mnt/c/Unity/release"
NODE_ROOT = "/var/www/html/node"

if __name__ == "__main__":
    print({"client": CLIENT_ROOT, "node": NODE_ROOT}[sys.argv[1]])
