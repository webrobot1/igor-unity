// Ведомая копия. Источник — /var/www/html/game/.claude/skills/artifact-check/anchor.cs в серверном
// репозитории; копию перезаписывает его хук при каждой правке источника. Правка копии теряется
// молча — вносить её в источник. Серверного репозитория нет под рукой → назвать нужную правку
// пользователю.

// Файл-якорь свода `artifact-check`. Продуктом не является, удалению и снятию из `paths` не
// подлежит.
// Свод, у которого в `paths` одни маски, в перечень доступных не встаёт: по имени приходит
// «Unknown skill» даже после чтения файла под его маску (открытый баг Claude Code #49835).
// Прямой путь без маски такой отказ снимает — им этот файл и объявлен.
// Маска свода остаётся на месте, якорь её не заменяет.
// Чтение файла окном в одну строку поднимает свод `artifact-check` и соседей, чья маска накрывает
// `.cs`; дальше они грузятся по имени.
