// Ведомая копия. Источник — /var/www/html/game/.claude/skills/csharp/anchor.cs в серверном
// репозитории; копию перезаписывает его хук при каждой правке источника. Правка копии теряется
// молча — вносить её в источник. Серверного репозитория нет под рукой → назвать нужную правку
// пользователю.

// Файл-якорь. Продуктом не является, компилятором не собирается, удалению не подлежит.
// Свод с объявленным `paths` в перечень доступных не встаёт, пока в сессии не прочитан файл под его
// маску (открытый баг Claude Code #49835). Маска считается от корня проекта, а своих .cs-файлов у
// серверного проекта нет: клиент Unity лежит вне его — без этого файла касание нечем сделать.
// Чтение файла поднимает все своды, чья маска накрывает .cs; дальше они грузятся по имени.
