#if UNITY_EDITOR
using UnityEngine;

namespace Mmogick
{
    /// <summary>
    /// Носитель корутины оснастки съёмки роликов (Assets/Plugins/Mmogick/VideoRig). Сама оснастка лежит
    /// в редакторной сборке, но повесить её поведение на объект сцены движок не даёт: компонент ищется по
    /// скрипту рантайм-сборки, и добавление редакторного класса возвращает пустоту — без носителя отсюда
    /// оснастке пришлось бы идти шагами редакторного цикла, а не кадрами игры, тогда как ввод игра читает
    /// раз в кадр. Оттого носитель и держит собственную рантайм-сборку в подкаталоге Runtime самой
    /// оснастки.
    ///
    /// Своего поведения носитель не несёт: он только крутит переданную ему корутину. В сборку игрока не
    /// попадает — файл целиком под UNITY_EDITOR.
    /// </summary>
    public class ScenarioHost : MonoBehaviour
    {
    }
}
#endif
