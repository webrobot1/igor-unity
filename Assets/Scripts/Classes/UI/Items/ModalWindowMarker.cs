using UnityEngine;

namespace Mmogick
{
	// Метка «это окно ждёт ответа игрока», вешается на панель модального окна тем контроллером,
	// который его ведёт (QuantityPromptController.Awake).
	// Общее закрытие меню (UIController.CloseAllMenu) такие окна не гасит: вопрос поднимает то же
	// действие игрока, после которого меню и закрываются, — без метки окно исчезало бы в том же кадре,
	// в котором открылось, а ответа не приходило бы вовсе. Закрывает такое окно только его собственный
	// ответ (подтверждение либо отказ).
	public class ModalWindowMarker : MonoBehaviour
	{
	}
}
